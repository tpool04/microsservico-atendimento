# Disable Kafka listeners during unit tests to avoid trying to connect to Kafka brokers
kafka.listener.auto-startup=false
# Disable JWT filter in tests to allow controller tests to run with @WithMockUser
security.jwt.enabled=false
# Use a simple secret for tests if JWT is required by security configuration (optional)
#jwt.secret=test-secret