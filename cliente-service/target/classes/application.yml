server:
  port: 8081

spring:
  application:
    name: cliente-service

  datasource:
    url: jdbc:postgresql://localhost:5432/atendimentosapi
    username: postgres
    password: coti
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

atendimento-service:
  url: http://atendimento-service:8083
