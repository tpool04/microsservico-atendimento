# Test configuration: do not send endereco via Kafka during tests
app.endereco.via-kafka=false
# Provide a default URL for the cliente-service used by tests (can be overridden)
cliente-service.url=http://localhost:8080
